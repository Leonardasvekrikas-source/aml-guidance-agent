# Pinned. A portfolio repo that installs differently next month is not
# reproducible, and every number in the README depends on reproducibility.

# --- database ---
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- retrieval ---
rank-bm25==0.2.2
# torch >= 2.6 is a hard requirement, not a preference: transformers refuses
# torch.load on older versions (CVE-2025-32434), which blocks any model whose
# weights are not published as safetensors - bge-m3 among them.
torch==2.13.0
sentence-transformers==6.0.0
transformers==5.16.1

# --- ingestion ---
pypdf==5.1.0
httpx==0.28.1
PyYAML==6.0.2

# --- generation ---
anthropic==0.40.0

# --- service ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1

# --- results formatting ---
tabulate==0.9.0

# --- tests ---
pytest==8.3.4
